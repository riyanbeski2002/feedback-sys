---
phase: 02-feedback-loop
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/app/(dashboard)/feedback/success/page.tsx
  - src/features/feedback/components/duplicate-error.tsx
autonomous: false
requirements: [COL-04]
---

<objective>
Finalize the feedback flow with success states and duplicate prevention.

Purpose: Provide a professional end-to-end user experience.
Output: Verified success flow with auto-redirect and error handling.
</objective>

<execution_context>
@/Users/User/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/User/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feedback-loop/02-CONTEXT.md
@.planning/phases/02-feedback-loop/02-RESEARCH.md
@.planning/phases/02-feedback-loop/02-01-SUMMARY.md
@.planning/phases/02-feedback-loop/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Success Page and Redirect</name>
  <files>src/app/(dashboard)/feedback/success/page.tsx</files>
  <action>
    Create a `SuccessPage` with a "Thank you" message.
    Add a supportive subtext: "Your feedback helps colleagues make better choices."
    Implement a 3-second auto-redirect to the `/hotels` page using a client-side component.
    Include a manual "Continue to Hotels" button for immediate navigation.
  </action>
  <verify>Submit feedback and confirm automatic redirect after 3 seconds.</verify>
  <done>Post-submission flow is smooth and helpful.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Duplicate Prevention UI</name>
  <files>src/features/feedback/components/duplicate-error.tsx, src/app/(dashboard)/feedback/[bookingId]/page.tsx</files>
  <action>
    Modify the feedback page to check if `feedback_submitted` is already true.
    If true, show a `DuplicateError` component instead of the form.
    Message: "You have already submitted feedback for this stay."
    Add a link back to the Bookings list.
  </action>
  <verify>Attempt to access a feedback URL for an already submitted booking and confirm error message.</verify>
  <done>System robustly prevents double-submission UI states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-End Feedback Loop Verification</name>
  <what-built>End-to-end feedback path: Booking Simulation -> Feedback Form -> Success -> Hotels List.</what-built>
  <how-to-verify>
    1. Navigate to `/bookings`.
    2. Click "Simulate Checkout" on a 'booked' stay.
    3. Confirm the simulation in the dialog.
    4. Click the "Rate Now" link that appears.
    5. Fill out the feedback form with various star ratings.
    6. Add a comment and click "Submit".
    7. Verify you see the "Thank you" page and get redirected to `/hotels`.
    8. Confirm the `feedback` table in Supabase has the new record with a correctly weighted score.
  </how-to-verify>
  <resume-signal>Type "feedback loop verified" or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
Ensure validation errors show correctly if stars are missing.
Confirm revalidatePath properly updates the bookings table state.
</verification>

<success_criteria>
- [ ] Success page auto-redirects.
- [ ] Duplicate submissions are blocked with clear UI.
- [ ] End-to-end loop verified by human.
</success_criteria>

<output>
After completion, create `.planning/phases/02-feedback-loop/02-03-SUMMARY.md`
</output>
