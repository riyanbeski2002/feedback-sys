---
phase: 07-design-mockups
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - .planning/designs/feedback-intel.pen
  - .planning/designs/04-admin-happy.png
  - .planning/designs/05-settings-happy.png
  - .planning/designs/05-settings-no-config.png
  - .planning/designs/05-settings-save-error.png
autonomous: true
requirements:
  - DSG-01

must_haves:
  truths:
    - "An Admin (happy path) frame exists showing 4 metric cards, a Flagged Hotels table, and a Live Activity feed with realistic seed data"
    - "A Settings (happy path — config loaded) frame exists showing the config form with correct weights and trigger fields"
    - "A Settings (no config state) frame exists showing an empty state with seed command instruction"
    - "A Settings (save error state) frame exists showing a destructive error toast or inline error"
    - "All 4 frames exported as PNG to .planning/designs/"
  artifacts:
    - path: ".planning/designs/04-admin-happy.png"
      provides: "Admin dashboard visual reference for Phase 8 (metric card colors DSG-04)"
    - path: ".planning/designs/05-settings-happy.png"
      provides: "Settings happy path reference"
    - path: ".planning/designs/05-settings-no-config.png"
      provides: "Settings empty state reference"
    - path: ".planning/designs/05-settings-save-error.png"
      provides: "Settings error state reference"
  key_links:
    - from: "App Shell frame (07-01 SUMMARY)"
      to: "Admin and Settings frames"
      via: "batch_design Copy (C) using shell node ID"
      pattern: "C(shell-node-id, admin-frame-id)"
---

<objective>
Design the Admin dashboard and all three Settings screen states using the App Shell from Plan 01, then export all four frames as PNG files.

Purpose: Admin and Settings are the management-side screens. The Admin mockup establishes the design contract for DSG-04 (metric card colors). The three Settings states lock the error and empty patterns for Phase 8.
Output: 4 designed frames + 4 PNG exports in `.planning/designs/`.
</objective>

<execution_context>
@/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-design-mockups/07-CONTEXT.md
@.planning/phases/07-design-mockups/07-RESEARCH.md
@.planning/phases/07-design-mockups/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design Admin dashboard (happy path)</name>
  <files>.planning/designs/feedback-intel.pen</files>
  <action>
    Open `.planning/designs/feedback-intel.pen` (or confirm active). Read `07-01-SUMMARY.md` for the App Shell node ID.

    **Frame: Admin Happy Path (1440×900px)**

    Build in 3 `batch_design` calls (≤25 ops each):

    Layer 1 — Frame + Shell:
    - Create frame: name="Admin - Happy Path", width=1440, height=900, background=#F8FAFA
    - Copy App Shell into frame using shell node ID from 07-01-SUMMARY.md
    - Update active nav item to show "Dashboard" active (teal accent bg #E8F9F7, text color #267268)
    - Header breadcrumb update: "Admin Dashboard" as the second line

    Layer 2 — Metric Cards Row:
    - Section title: "Feedback Health" (x=264, y=100, size=20px, bold, color=#181A1A)
    - 4 metric cards in a row, x=264, y=136, width=264px each, height=100px, gap=16px, white bg, rounded-xl, border 1px #E9ECEC, shadow-sm
    - Card 1 — "Total Feedbacks": icon (message-square, color=#72D3C4) + value "14" (size=32px, bold) + label "Total Submissions" (size=12px, color=#717373). Top-left accent: teal left-border 3px or top-border 3px
    - Card 2 — "Platform Avg Score": icon (star, color=#72D3C4) + value "3.6" (size=32px, bold) + label "Average Score" + badge "Stable" in teal pill
    - Card 3 — "Flagged Hotels": icon (alert-triangle, color=#F08080) + value "1" (size=32px, bold, color=#F08080) + label "Below 2.0 threshold". Top accent: destructive red left-border 3px
    - Card 4 — "Total Hotels": icon (building, color=#72D3C4) + value "6" (size=32px, bold) + label "Active Properties"

    Note for DSG-04: Use design system colors (teal/destructive from variables) NOT hardcoded yellow (#F59E42) or blue (#3B82F6). Each card accent uses either primary teal or destructive red based on sentiment. No inline hex overrides.

    Layer 3 — Flagged Hotels Table + Live Activity:
    - Two-column layout: left 2/3 (width=752px) + right 1/3 (width=368px), x=264, y=256, gap=20px
    - Flagged Hotels section (left):
      - Section title: "Flagged Hotels" + alert-triangle icon teal (x=264, y=256)
      - Table container: white bg, rounded-xl, border 1px #E9ECEC
      - Table headers: Hotel | Score | Flag Reason | Action (size=12px, #717373, uppercase)
      - 1 flagged row: Hotel Raj Palace Jaipur | 1.4/5.0 | Cleanliness 1.0, Service 1.2 | "Review" button (destructive outline, rounded-full)
      - Below table: "Export Report" button (rounded-full, primary teal) + "System Config" button (rounded-full, outline) side by side
    - Live Activity section (right):
      - Section title: "Live Activity" + activity icon
      - Feed card: white bg, rounded-xl, border 1px #E9ECEC, padding=16px
      - 4 activity items with timestamp, traveller name, hotel, and sentiment tag:
        1. "2h ago — Priya Sharma submitted feedback for Grand Hyatt Mumbai" + POSITIVE pill (#E8F9F7, #267268)
        2. "4h ago — Arjun Mehta submitted feedback for Hotel Raj Palace Jaipur" + URGENT pill (#FDE8E8, #B03030)
        3. "6h ago — Kavitha Nair submitted feedback for The Leela Delhi" + POSITIVE pill
        4. "8h ago — Rohan Verma submitted feedback for ITC Maurya New Delhi" + STABLE pill (#F2F4F4, #717373)

    Call `get_screenshot()` on Admin Happy Path frame. Verify: 4 metric cards visible, Flagged Hotels table shows Hotel Raj Palace Jaipur in red, activity feed visible on right, no hardcoded yellow/blue colors.
  </action>
  <verify>
    <automated>Call `get_screenshot()` on "Admin - Happy Path" frame. Confirm 4 metric cards are visible, Flagged Hotels table shows Hotel Raj Palace Jaipur, Live Activity feed is visible on the right side.</automated>
  </verify>
  <done>Admin Happy Path frame designed and screenshot-validated. Metric cards use teal/destructive design tokens (no hardcoded yellow/blue). Flagged Hotels table and Live Activity feed populated with realistic seed data.</done>
</task>

<task type="auto">
  <name>Task 2: Design Settings screens (3 states) and export all 4 Admin/Settings frames</name>
  <files>
    .planning/designs/feedback-intel.pen
    .planning/designs/04-admin-happy.png
    .planning/designs/05-settings-happy.png
    .planning/designs/05-settings-no-config.png
    .planning/designs/05-settings-save-error.png
  </files>
  <action>
    **Frame A: Settings Happy Path — Config Loaded (1440×900px)**

    Build in 2 layers (≤25 ops each):

    Layer 1 — Frame + Shell:
    - Create frame: name="Settings - Happy Path", width=1440, height=900, background=#F8FAFA
    - Copy App Shell, set Settings as active nav item

    Layer 2 — Settings Form:
    - Page title: Settings icon (size=24, color=#72D3C4) + "System Configuration" (x=264, y=100, size=24px, bold, color=#181A1A)
    - Form card: x=264, y=140, width=800, white bg, rounded-xl, border 1px #E9ECEC, padding=32px
    - Section header: "Scoring Weights" (size=16px, bold, color=#181A1A, y=172) + description "Weights must sum to 100%" (size=13px, color=#717373)
    - 4 weight fields in 2×2 grid (gap=16px, starting y=208):
      - Cleanliness Weight: label + number input showing "30" + "%" suffix
      - Service Weight: label + number input showing "30" + "%"
      - Value Weight: label + number input showing "20" + "%"
      - Amenities Weight: label + number input showing "20" + "%"
    - Divider line at y≈320
    - Section header: "Trigger Settings" (size=16px, bold, y=336)
    - Trigger Delay field: label "Delay after checkout (hours)" + input showing "24"
    - Reminder Frequency field: label "Reminder every (hours)" + input showing "48"
    - Divider line
    - Section header: "Thresholds"
    - Flagging Threshold field: label "Flag hotels below score" + input showing "2.0"
    - Bottom row: "Save Changes" button (rounded-full, primary teal, height=44px) + "Reset to Defaults" button (rounded-full, outline, ghost)

    **Frame B: Settings No Config State (1440×900px)**

    Build in 2 layers (≤25 ops):

    Layer 1 — Frame + Shell (Settings active)

    Layer 2 — Empty State:
    - Centered in content area: Wrench icon (80×80, color=#E9ECEC)
    - Heading: "No Configuration Found" (size=20px, bold, color=#181A1A)
    - Subtext: "Run the seed script to initialize default settings." (size=14px, color=#717373)
    - Code hint pill: `npm run seed` in monospace, background=#F2F4F4, rounded-md

    **Frame C: Settings Save Error State (1440×900px)**

    Build in 2 layers (≤25 ops):

    Layer 1 — Frame + Shell (Settings active)

    Layer 2 — Form with error toast:
    - Show the same form as Settings Happy Path (can copy the form structure)
    - Add an error toast/banner at top of content area (x=264, y=88, width=1152, height=56):
      - Background: #FDE8E8 (destructive light), border 1px #F08080, rounded-lg
      - AlertCircle icon (color=#F08080, size=20) + "Error Loading Configuration" (size=14px, bold, color=#B03030) + "Unable to save — please try again." (size=13px, color=#B03030)
      - X dismiss button on right

    **Export all 4 frames:**

    Call `export_nodes`:
    - nodeIds: [admin-happy-id, settings-happy-id, settings-no-config-id, settings-error-id]
    - format: "PNG"
    - folder: "/Users/User/Documents/feedback-sys/.planning/designs/"

    Take `get_screenshot()` on each Settings frame to validate the form layout, empty state, and error toast are visually correct before exporting.
  </action>
  <verify>
    <automated>Run `ls /Users/User/Documents/feedback-sys/.planning/designs/*.png` and confirm 8 PNG files total now exist (4 from Plan 02 + 4 from this plan). Call `get_screenshot()` on each Settings frame to confirm: happy path shows form with weights, no-config shows empty state, save-error shows destructive toast/banner.</automated>
    <manual>Confirm Settings happy path form shows Cleanliness=30%, Service=30%, Value=20%, Amenities=20% — these must match the seeded feedback_config values exactly.</manual>
  </verify>
  <done>3 Settings frames and 1 Admin frame designed and screenshot-validated. 4 PNG files exported. Admin metric cards use design system tokens. Settings form shows correct weight values.</done>
</task>

</tasks>

<verification>
1. 4 frames exist: Admin-Happy, Settings-Happy, Settings-No-Config, Settings-Save-Error.
2. 4 PNG files exported: 04-admin-happy.png, 05-settings-happy.png, 05-settings-no-config.png, 05-settings-save-error.png.
3. Admin metric cards use teal/destructive design tokens — no hardcoded yellow or blue.
4. Settings form shows correct seeded weight values (30/30/20/20).
5. Save-error state shows clearly destructive (#F08080) styled error.
</verification>

<success_criteria>
- 4 PNG exports in .planning/designs/ (8 total across Plans 02-03)
- Admin metric card colors establish Phase 8 DSG-04 contract
- Settings error state clearly shows destructive styling pattern
- All three Settings states are visually distinct (form loaded vs empty vs error)
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-mockups/07-03-SUMMARY.md` containing:
- Node IDs of all 4 frames
- Confirmation of 4 PNG export paths
- Note on Admin metric card color decisions (for Phase 8 DSG-04 reference)
- Settings form field dimensions for Phase 8 component sizing
</output>
