---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .env.local
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/middleware.ts
  - src/middleware.ts
  - src/scripts/seed.ts
autonomous: true
requirements: [SCI-03]
user_setup:
  - service: supabase
    why: "Primary database and auth provider"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API"
---

<objective>
Setup Supabase client-side and server-side integration and seed the initial data.

Purpose: Enable data-driven features and real-time updates.
Output: Working Supabase connection, auth middleware, and seeded hotel/booking data.
</objective>

<execution_context>
@/Users/User/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/User/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Supabase SSR Client and Middleware</name>
  <files>src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/lib/supabase/middleware.ts, src/middleware.ts</files>
  <action>
    Install `@supabase/ssr` and `@supabase/supabase-js`.
    Create a `createClient` function in `src/lib/supabase/server.ts` using the Next.js 16 async cookies API.
    Create a `createClient` function in `src/lib/supabase/client.ts` for browser-side usage.
    Add middleware logic in `src/lib/supabase/middleware.ts` to refresh the session.
    Setup `src/middleware.ts` to execute the session refresh.
  </action>
  <verify>Check console for session refresh logs (if added) and ensure no cookie errors.</verify>
  <done>Supabase clients are ready for use in Server and Client components.</done>
</task>

<task type="auto">
  <name>Task 2: Seed Database Schema and Data</name>
  <files>src/scripts/seed.ts</files>
  <action>
    Create a seed script that populates the database using the SQL schema from `basic/09_database_schema.sql`.
    Add at least 5-10 hotels across Bangalore, Mumbai, and Delhi.
    Add a mix of 'booked', 'checked_in', and 'completed' bookings for demo purposes.
    Ensure `feedback_config` is initialized with default weights (30% Cleanliness, 30% Service).
    Include `users` and `profiles` for the travel management context.
  </action>
  <verify>Run `node src/scripts/seed.ts` and verify data in Supabase dashboard.</verify>
  <done>Database is populated with realistic test data for the feedback loop.</done>
</task>

</tasks>

<verification>
Check `supabase.auth.getUser()` in a simple Server Component to verify auth connection.
Verify `hotels` table has seeded rows.
</verification>

<success_criteria>
- [ ] Supabase SSR integration complete.
- [ ] Database seeded with hotels, bookings, and configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
