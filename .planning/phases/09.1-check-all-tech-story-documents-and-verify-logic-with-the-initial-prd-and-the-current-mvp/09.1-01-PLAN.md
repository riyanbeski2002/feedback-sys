---
phase: 09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/VERIFICATION-REPORT.md
  - .planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md
autonomous: true
requirements:
  - TRG-01
  - TRG-02
  - TRG-03
  - TRG-04
  - COL-01
  - COL-02
  - COL-03
  - COL-04
  - SCI-01
  - SCI-02
  - SCI-03
  - SCI-04
  - DSB-01
  - DSB-02
  - DSB-03

must_haves:
  truths:
    - "VERIFICATION-REPORT.md exists and contains a summary table with one row per Tech Story showing Aligned / Minor gaps / Major gaps / Planned, not built"
    - "Every PRD requirement (TRG-01–04, COL-01–04, SCI-01–04, DSB-01–03) appears in the PRD coverage section confirming Tech Story traceability"
    - "3 Critical gaps (COL-03, SCI-04, TRG-03) are documented with recommended actions and corresponding TS-14/15/16 stories are added to TECH-STORIES.md"
    - "Circular dependencies in TS-9/TS-10/TS-11 and TS-12 are identified in the report and corrected in TECH-STORIES.md"
    - "TS-13 sentiment_confidence column name is corrected to sentiment_score in TECH-STORIES.md"
    - "TECH-STORIES.md edits are limited to cosmetic fixes and TS-14+ additions — no story scope, effort estimates, or acceptance criteria are changed"
  artifacts:
    - path: ".planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/VERIFICATION-REPORT.md"
      provides: "Structured gap analysis report with summary table and per-story findings"
      min_lines: 120
    - path: ".planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md"
      provides: "Corrected Tech Stories document with cosmetic fixes and 3 new gap-closure stories"
      contains: "[ADDED — GAP CLOSURE]"
  key_links:
    - from: "VERIFICATION-REPORT.md"
      to: "TECH-STORIES.md"
      via: "Critical gaps trigger new TS-14+ story additions"
      pattern: "ADDED — GAP CLOSURE"
    - from: "REQUIREMENTS.md"
      to: "VERIFICATION-REPORT.md"
      via: "PRD coverage section lists all 31 requirements and their story coverage"
      pattern: "COL-03|SCI-04|TRG-03"
---

<objective>
Cross-check all 13 Tech Stories against the PRD (REQUIREMENTS.md) and MVP codebase, produce a structured VERIFICATION-REPORT.md, then apply safe auto-corrections directly to TECH-STORIES.md.

Purpose: Ensure the Tech Story document accurately reflects the system as designed (PRD) and as built (MVP), so future developers reading TECH-STORIES.md are not misled by contradictions, circular dependencies, or missing coverage.

Output:
- VERIFICATION-REPORT.md — stakeholder-readable gap analysis report
- Updated TECH-STORIES.md — 3 cosmetic fixes + 3 new TS-14/15/16 stories for critical gaps
</objective>

<execution_context>
@/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/09.1-CONTEXT.md
@.planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/09.1-RESEARCH.md

@.planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md

# MVP source files for spot-checking story claims against implementation
@src/features/feedback/actions/submit-feedback.ts
@src/features/feedback/lib/scoring.ts
@src/features/feedback/lib/analysis.ts
@src/features/admin/actions/update-config.ts
@src/features/bookings/actions/simulate-checkout.ts
@src/app/(dashboard)/admin/page.tsx
@src/features/notifications/components/notifications-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write VERIFICATION-REPORT.md</name>
  <files>.planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/VERIFICATION-REPORT.md</files>
  <action>
Create VERIFICATION-REPORT.md in the phase directory. The report must be readable by a non-technical stakeholder and contain these sections in order:

**Section 1 — Preamble**
List the three source documents used for verification: REQUIREMENTS.md (PRD, 31 active requirements), TECH-STORIES.md (13 stories, v1.0 MVP), and the MVP codebase (Next.js/Supabase). State the date of verification and that the MVP was built as a prototype with inline synchronous operations rather than the queue-based architecture described in most stories.

**Section 2 — Summary Table**
One row per story. Columns: Story ID | Title | Status | Notes (brief).
Status values (use exactly these): `Aligned` / `Minor gaps` / `Planned, not built` / `Critical gap`.

Based on the research findings, the expected statuses are:
- TS-1: Minor gaps (no scheduled backfill job in MVP; real-time path exists)
- TS-2: Planned, not built (task queue / trigger system not in MVP; TRG-04 is partial only)
- TS-3: Planned, not built (no real channel adapters; UI preview only; NOT-01 deferred)
- TS-4: Minor gaps (route differs, 1-5 stars not 1-10 sliders, "Location" dimension vs. PRD "Intent")
- TS-5: Minor gaps (server action not REST endpoint; scores inline; no signed token)
- TS-6: Minor gaps (table names and schema differ; PRD satisfied by denormalized equivalent)
- TS-7: Minor gaps (inline function not worker; weights hardcoded not DB-driven at compute time)
- TS-8: Minor gaps (no hotel_rankings table; ranking via client-side sort only)
- TS-9: Minor gaps (10-point scale thresholds vs. 5-point scale in MVP)
- TS-10: Minor gaps (no RBAC, no manual override; AUTH-01 deferred)
- TS-11: Minor gaps (implicit flagging only; no flag/unflag API; no flagged_hotels table)
- TS-12: Minor gaps (weight validation rule differs; no active halt; recalculation inline)
- TS-13: Minor gaps (synchronous; produces 4 fields not 2; column name difference)

**Section 3 — Critical Gap Analysis**
Three critical gaps identified (PRD requirements with no corresponding Tech Story):

1. **COL-03 — WhatsApp Quick Score** (PRD: COL-03): No Tech Story covers the standalone WhatsApp-style quick 1-10 score input as a distinct submission mechanism. TS-3 covers delivery; TS-4 covers the full detailed form. The simplified score-only experience is unaddressed. Recommended action: Add TS-14 [ADDED — GAP CLOSURE].

2. **SCI-04 — Automatic Hotel Flagging** (PRD: SCI-04): Neither TS-9 nor TS-11 describes the automated threshold-based flagging that triggers when avg_score drops below 2.0. TS-9 shows a Flagged badge and TS-11 covers admin-initiated flag/unflag, but automatic flagging on score drop is owned by no story. The MVP implements this inline in submit-feedback.ts (status_bucket = "flagged" when avg < 2.0). Recommended action: Add TS-15 [ADDED — GAP CLOSURE].

3. **TRG-03 — Notification Preview UI** (PRD: TRG-03): TS-3 describes real channel delivery workers (SendGrid, WhatsApp Business API, Slack, Teams webhook). The MVP built a notification preview page showing rendered channel mockups — this is the actual delivered feature for TRG-03. No story describes the preview UI architecture. Recommended action: Add TS-16 [ADDED — GAP CLOSURE].

**Section 4 — Per-Story Detailed Findings**
For each story TS-1 through TS-13, write a short block with:
- Status (from summary table)
- What the story says
- What the MVP built
- Gap detail (list each gap as: [Severity] description)
- Whether PRD requirements are satisfied

Use these severity labels: [Critical] / [Minor] / [Cosmetic] / [Planned, not built]

Key findings to document per story:

TS-1: [Minor] No scheduled backfill job exists — only the real-time `simulateCheckout` server action. PRD TRG-01/TRG-02 satisfied. TS-1/TS-2 timing contradiction already resolved in document (confirm this).

TS-2: [Planned, not built] No task queue, no `SEND_FEEDBACK_REQUEST` tasks, no `trigger_sent` flag, no reminder scheduling. PRD TRG-04 partially satisfied (delay/frequency mocked). Route as NOT-01 deferred.

TS-3: [Planned, not built] No SendGrid/WhatsApp/Slack/Teams adapters. Notification page is a preview UI (see Critical gap TRG-03 → TS-16). PRD TRG-03 satisfied by the preview UI built, but the story scope does not match what was built.

TS-4: [Minor] Route `/feedback/[bookingId]` not `/feedback?token=...`; no signed token; 1–5 star ratings not 1–10 sliders; [Cosmetic] 5th dimension "Location" in story but PRD says "Intent" and MVP uses "Recommend to colleagues".

TS-5: [Minor] Server action `submitFeedback` not REST endpoint; no HTTP 201/401/409 status codes; scoring computed inline; no `COMPUTE_SCORE` task enqueue.

TS-6: [Minor] Table name `feedback` not `feedback_submissions`; no separate `hotel_scores`, `hotel_rankings`, `feedback_triggers`, `flagged_hotels`, `notifications`, `admin_overrides` tables — MVP uses denormalized columns on `hotels` and `bookings`.

TS-7: [Minor] `calculateWeightedScore()` runs inline in `scoring.ts`; weights come from `feedback_config` at server action time (not DB-configurable per-compute); updates `hotels.avg_score` directly.

TS-8: [Minor] No `hotel_rankings` table; `calculateWeightedRanking()` exists in scoring.ts but is not persisted; hotels page sorts by `avg_score` client-side. PRD DSB-01 satisfied.

TS-9: [Minor] Tier thresholds reference 10-point scale (Top Rated ≥8.0) vs. MVP 5-point scale (top_rated ≥4.5); tier labels match. [Minor] Dependency graph: TS-9 lists TS-11 as dependency and TS-11 lists TS-9 — circular dependency (see Section 5).

TS-10: [Planned, not built] No RBAC (AUTH-01 deferred); no manual score override; no `admin_overrides` table. MVP shows metric cards and flagged hotel list. PRD DSB-02 satisfied by the metrics display.

TS-11: [Minor] No explicit flag/unflag API endpoints; no `flagged_hotels` table. Flagging is implicit via `status_bucket = 'flagged'` in the `hotels` table. [Minor] Circular dependency with TS-9/TS-10 (see Section 5).

TS-12: [Minor] Weight validation: MVP requires weights sum to exactly 1.0; TS-12 specifies each weight in range 0.0–5.0 independently (different constraint, both prevent degenerate input). [Minor] No `active` system halt column in MVP `feedback_config`. [Minor] `RECOMPUTE_ALL_SCORES` runs inline in `update-config.ts` server action, not via task.

TS-13: [Minor] `analyzeFeedback()` runs synchronously inline in `submit-feedback.ts`, not via async task. [Cosmetic] Column name: story says `sentiment_confidence`, MVP uses `sentiment_score`. [Minor] MVP produces 4 fields (`sentiment_label`, `sentiment_score`, `issue_category`, `urgency_flag`) — richer than story's 2 fields (`sentiment_label`, `sentiment_confidence`). PRD SCI-02 satisfied.

**Section 5 — Dependency Chain Analysis**
Document the three dependency issues found in the Story Summary table:

1. **TS-4 ↔ TS-5 mutual listing** — TS-4 lists TS-5 as a dependency and TS-5 lists TS-4 as a dependency. This is a producer-consumer relationship (form calls the API), not a genuine circular build dependency. TS-5's dependency on TS-4 should be read as "called by" not "depends on to be built." Classification: Minor — misleading direction, not a build blocker.

2. **TS-9 / TS-10 / TS-11 triangle** — TS-11 lists TS-9 and TS-10 as dependencies; TS-9 and TS-10 both list TS-11 as a dependency. Genuine circular dependency: TS-11 cannot be built until TS-9 and TS-10 are built, but TS-9 and TS-10 cannot be built until TS-11 is built. Resolution: TS-11 depends on TS-6 for storage only. TS-9 and TS-10 consume TS-11's output but TS-11 does not depend on them to implement the flag API. TS-11's §6 Dependencies listing of TS-9 and TS-10 should be removed. Classification: Minor — causes implementation ordering confusion.

3. **TS-12 ↔ TS-2 reverse listing** — TS-12 lists TS-2 as a dependency; TS-2 lists TS-12 as a dependency. TS-2 reads config values from TS-12 at runtime (TS-2 depends on TS-12). TS-12 does not depend on TS-2 to be built. TS-12's listing of TS-2 is a reverse dependency error. Classification: Minor — misleading direction.

**Section 6 — PRD Coverage Confirmation**
Table listing all 31 PRD requirements (v1 + v2 active) with columns: Req ID | Description | Covered by Story | Notes.

For v2 DSG/FND/DAT/FTR requirements: note that TECH-STORIES.md was scoped to the functional feedback intelligence loop (v1 requirements). Design token, Tailwind, and UI polish requirements (DSG-01–06, FND-01–04, DAT-01–04, FTR-01–02) are intentionally outside TECH-STORIES.md scope — this is not a gap.

For v1 requirements with Critical gaps (COL-03, SCI-04, TRG-03): flag as "Gap — TS-14/15/16 added."

**Section 7 — Pre-Resolved Issues**
Confirm the TS-1/TS-2 timing contradiction is resolved: TS-1 §3 FR-6 and TS-2 §8 Edge Cases both describe the "Late detection by backfill job" scenario with correct fire-time arithmetic. No re-flagging needed.

**Section 8 — Recommended Actions Summary**
Numbered list of all recommended actions organized by severity:
- Critical: add TS-14, TS-15, TS-16 (already done in Task 2)
- Minor: 13 story deviations documented above (no file changes required)
- Cosmetic: sentiment_confidence → sentiment_score in TS-13 §4 (already fixed in Task 2); TS-11 dependency list cleaned; TS-12 dependency list cleaned
  </action>
  <verify>
    <automated>test -f /Users/User/Documents/feedback-sys/.planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/VERIFICATION-REPORT.md && wc -l /Users/User/Documents/feedback-sys/.planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/VERIFICATION-REPORT.md</automated>
    <manual>Confirm the report has: summary table with 13 rows, Critical gap section with 3 entries (COL-03/SCI-04/TRG-03), dependency chain section with 3 findings, PRD coverage section listing all 31 requirements.</manual>
  </verify>
  <done>VERIFICATION-REPORT.md exists with all 8 sections. Summary table has one row per story (TS-1 through TS-13). All 3 critical gaps are named with recommended actions. All 3 circular/reverse dependency issues are documented. PRD coverage section confirms all 31 requirements are addressed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply auto-corrections to TECH-STORIES.md</name>
  <files>.planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md</files>
  <action>
Apply exactly these four categories of changes to TECH-STORIES.md. Do NOT change story scope, effort estimates, functional requirements text, or acceptance criteria — those are Minor gap documentation only.

**Cosmetic Fix 1 — TS-13 column name alignment**
Find the section in TS-13 (Sentiment Analysis Layer) that references the output column `sentiment_confidence`. Change it to `sentiment_score` to match the actual MVP database column name. This appears in §4 (Data Model / Schema), likely in a table or field listing. The change is: `sentiment_confidence` → `sentiment_score`.

**Dependency Fix 2 — TS-11 circular dependency removal**
Find TS-11 (Flagging and Blacklisting Logic) §6 Dependencies. Remove "TS-9: Hotel Listing Integration" and "TS-10: Admin Dashboard" from the dependencies list. TS-9 and TS-10 are consumers of TS-11, not prerequisites for building TS-11. TS-11's only true build dependency is TS-6 (data storage). Keep TS-6 in the list.

**Dependency Fix 3 — TS-12 reverse dependency removal**
Find TS-12 (Configuration System) §6 Dependencies. Remove "TS-2: Feedback Trigger System" from the list. TS-2 reads config values from TS-12 at runtime — TS-12 does not depend on TS-2 to be built. TS-12's true dependencies are TS-6 (storage) and TS-7 (score recomputation).

**Critical Gap Additions — TS-14, TS-15, TS-16**
Append three new Tech Stories at the end of TECH-STORIES.md, after TS-13. Each story must be marked `[ADDED — GAP CLOSURE]` in its heading line. Update the **Total Stories** count in the document header from 13 to 16.

---

**TS-14 content (WhatsApp Quick Score):**

```
## Tech Story 14: WhatsApp Quick Score Submission [ADDED — GAP CLOSURE]

### 1. Objective
Provide a simplified score-only feedback path delivered via WhatsApp, allowing guests to submit a single satisfaction rating (1–10) without opening the full detailed feedback form. This satisfies PRD requirement COL-03.

### 2. Scope
**Included:**
- A WhatsApp-style UI mockup showing a single 1–10 score input (numeric or slider)
- Submission path that records the quick score against the booking in the feedback table
- Display of the submitted quick score alongside full feedback submissions in admin views

**Excluded:**
- Actual WhatsApp Business API integration (see TS-3 and NOT-01 deferred)
- Comment or dimension breakdown (handled by TS-4 full form)

### 3. Functional Requirements
1. Guest can input a satisfaction score from 1 to 10 via a single-input UI
2. Score is stored against the booking with a `submission_type = 'quick_score'` flag
3. Duplicate prevention applies: a booking with `feedback_submitted = true` cannot receive a second quick score
4. Quick score contributes to hotel aggregate score calculations (TS-7)
5. The notifications page can display a quick score submission in channel preview mockups

### 4. Non-Functional Requirements
- Response time for score submission < 2 seconds
- Score input UI is mobile-first (WhatsApp primary context)

### 5. Inputs & Outputs
**Inputs:**
- Booking ID, guest score (integer 1–10)

**Outputs:**
- Feedback row with `submission_type = 'quick_score'`, score stored, `feedback_submitted = true` on booking

### 6. Dependencies
- TS-4: Feedback Form UI (shares eligibility and duplicate-prevention logic)
- TS-5: Feedback Submission API (score persisted via same submission path)
- TS-6: Data Storage (feedback table)

### 7. Acceptance Criteria
- [ ] Guest can submit a score from 1–10 via the quick score UI
- [ ] Submission marks the booking as `feedback_submitted = true`
- [ ] A second submission attempt for the same booking is rejected
- [ ] Quick score is visible in admin feedback feed

### 8. Edge Cases
- Score outside 1–10 range: reject with validation error
- Booking already has full feedback submitted: reject with 409

### 9. Estimated Effort
1 day (2 hours UI + 4 hours backend + 2 hours testing)
```

---

**TS-15 content (Automatic Hotel Flagging):**

```
## Tech Story 15: Automatic Hotel Flagging on Score Threshold [ADDED — GAP CLOSURE]

### 1. Objective
Automatically update a hotel's status to "flagged" when its average score drops below a configurable critical threshold after any new feedback submission. This satisfies PRD requirement SCI-04.

### 2. Scope
**Included:**
- Threshold evaluation after every feedback submission and after every score recalculation
- Setting `status_bucket = 'flagged'` (or equivalent status field) when `avg_score < threshold`
- Clearing the flagged status when score recovers above the threshold
- Configurable threshold stored in `feedback_config` (default: 2.0 on a 1–5 scale)

**Excluded:**
- Admin-initiated manual flag/unflag (covered by TS-11)
- Blacklist enforcement (covered by TS-11)
- Sending alert notifications when a hotel is flagged (see TS-3)

### 3. Functional Requirements
1. After every feedback submission, the system evaluates `avg_score` against the configured `flagging_threshold`
2. If `avg_score < flagging_threshold`, set `status_bucket = 'flagged'`
3. If `avg_score >= flagging_threshold`, set `status_bucket` to the appropriate tier (top_rated / stable / needs_review)
4. After admin changes weights and triggers score recalculation, all hotels are re-evaluated against the threshold
5. The threshold is configurable via the admin Config panel (TS-12)

### 4. Non-Functional Requirements
- Threshold evaluation completes within the same request as the score update (synchronous)
- No hotel remains in an incorrect status bucket after a submission

### 5. Inputs & Outputs
**Inputs:**
- Updated `avg_score` for a hotel, `flagging_threshold` from `feedback_config`

**Outputs:**
- Updated `status_bucket` on the hotel row

### 6. Dependencies
- TS-6: Data Storage (hotels and feedback_config tables)
- TS-7: Scoring Engine (provides updated avg_score)
- TS-12: Configuration System (provides flagging_threshold)

### 7. Acceptance Criteria
- [ ] Hotel with avg_score 1.8 (below 2.0 threshold) shows status_bucket = 'flagged'
- [ ] Hotel with avg_score 2.1 (above 2.0 threshold) does not show 'flagged'
- [ ] After score recalculation, status_bucket is updated for all affected hotels
- [ ] Changing the threshold in Config and saving re-evaluates all hotels

### 8. Edge Cases
- Hotel with no feedback yet: status_bucket remains 'stable' (default)
- Threshold set to 0: no hotels can be flagged (valid config)
- Hotel score exactly equal to threshold: not flagged (strict less-than rule)

### 9. Estimated Effort
1 day (2 hours logic + 4 hours integration + 2 hours testing)
```

---

**TS-16 content (Notification Preview UI):**

```
## Tech Story 16: Multi-Channel Notification Preview UI [ADDED — GAP CLOSURE]

### 1. Objective
Provide an admin-facing notification preview page that renders how a feedback alert would appear in each supported channel (Email, WhatsApp, Slack, Teams) using real submitted feedback data. This satisfies PRD requirement TRG-03 as the prototype implementation of notification visibility.

### 2. Scope
**Included:**
- A notifications page with four channel preview panels (Email, WhatsApp, Slack, Teams)
- Each panel populated from the most recent real feedback submission (hotel name, traveller name, score, comment excerpt)
- A submission selector allowing the admin to preview notifications for any past submission
- Pixel-accurate channel mockup rendering matching each platform's visual style

**Excluded:**
- Actual message dispatch to external APIs (see TS-3 and NOT-01 deferred)
- Delivery status tracking or retry logic

### 3. Functional Requirements
1. Notifications page loads the most recent feedback submission by default
2. Admin can select any past submission from a dropdown to preview its notification content
3. All four channel panels update simultaneously when a submission is selected
4. Email panel renders subject line, header, body text, CTA button, and footer
5. WhatsApp panel renders a message bubble with score and comment excerpt
6. Slack panel renders a message card with attachment fields
7. Teams panel renders an Adaptive Card with hotel details and score
8. Channel previews use real data (hotel name, traveller name, score, comment) — not hardcoded placeholder text

### 4. Non-Functional Requirements
- Page load time < 2 seconds with up to 100 submissions in the selector
- Preview renders correctly on both desktop and mobile viewport widths

### 5. Inputs & Outputs
**Inputs:**
- Feedback submission record (hotel_name, traveller_name, avg_score, comment, submission_date)
- Selected submission ID from dropdown

**Outputs:**
- Rendered channel preview panels displaying real submission data

### 6. Dependencies
- TS-2: Feedback Trigger System (defines what a "notification" event represents)
- TS-3: Notification System (defines the content schema each channel expects)
- TS-5: Feedback Submission API (provides the submission data)

### 7. Acceptance Criteria
- [ ] Notifications page loads and shows the most recent submission's data in all 4 panels
- [ ] Changing the submission selector updates all 4 panels with the selected submission's data
- [ ] Each panel's hotel name, traveller name, score, and comment excerpt match the selected submission
- [ ] No hardcoded sample data appears when real submissions exist

### 8. Edge Cases
- No submissions exist yet: show placeholder content indicating no data available
- Comment field is empty: panel renders without comment excerpt, no layout break
- Very long hotel name or comment: truncate with ellipsis at channel-appropriate lengths

### 9. Estimated Effort
2 days (4 hours data layer + 6 hours UI components + 2 hours testing + 4 hours channel-specific styling)
```

After appending all three stories, update the document header line `**Total Stories:** 13` to `**Total Stories:** 16`.

Also update the Story Summary table at the end of TECH-STORIES.md (if one exists) to add rows for TS-14, TS-15, TS-16 with their PRD requirement coverage and dependencies.
  </action>
  <verify>
    <automated>grep -c "ADDED — GAP CLOSURE" /Users/User/Documents/feedback-sys/.planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md</automated>
    <manual>Confirm: (1) grep returns 3 matches, (2) "sentiment_score" appears in TS-13 data model section (not "sentiment_confidence"), (3) TS-11 §6 Dependencies no longer lists TS-9 or TS-10, (4) TS-12 §6 Dependencies no longer lists TS-2, (5) Total Stories header reads 16.</manual>
  </verify>
  <done>TECH-STORIES.md contains: 3 new stories marked [ADDED — GAP CLOSURE] (TS-14/15/16), TS-13 column name corrected to sentiment_score, TS-11 circular dependencies removed, TS-12 reverse dependency removed, Total Stories updated to 16.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify end state:

1. `test -f .planning/phases/09.1-.../VERIFICATION-REPORT.md` — file exists
2. `grep -c "ADDED — GAP CLOSURE" .planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md` — returns 3
3. `grep "sentiment_score" .planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md` — appears in TS-13 section
4. `grep -c "Total Stories.*16" .planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md` — returns 1
5. `grep "TS-9" .planning/quick/1-create-tech-story-document-for-post-stay/TECH-STORIES.md | grep "Dependencies"` — TS-9 should not appear in TS-11's Dependencies section
</verification>

<success_criteria>
Phase 09.1 complete when:
1. VERIFICATION-REPORT.md exists with all 8 sections: preamble, summary table (13 rows), critical gap analysis (3 gaps), per-story findings (TS-1 through TS-13), dependency chain analysis (3 findings), PRD coverage table (31 requirements), pre-resolved issues confirmation, recommended actions summary.
2. TECH-STORIES.md has exactly 3 new gap-closure stories (TS-14 WhatsApp Quick Score, TS-15 Automatic Hotel Flagging, TS-16 Notification Preview UI).
3. Three cosmetic/dependency corrections applied: sentiment_confidence → sentiment_score in TS-13, TS-9/TS-10 removed from TS-11 dependencies, TS-2 removed from TS-12 dependencies.
4. Total Stories count in document header updated from 13 to 16.
5. No Minor-gap story content (scope, effort, acceptance criteria) has been modified.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-check-all-tech-story-documents-and-verify-logic-with-the-initial-prd-and-the-current-mvp/09.1-01-SUMMARY.md`
</output>
