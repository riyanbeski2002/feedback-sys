---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components.json
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/(dashboard)/layout.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/header.tsx
autonomous: true
requirements: [SCI-03]
must_haves:
  truths:
    - "Next.js 16 app runs with Turbopack"
    - "Sidebar navigation is visible and collapsible"
    - "Theme toggle switches between light and dark modes"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with theme provider"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Collapsible dashboard navigation"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "React context"
---

<objective>
Initialize the Next.js 16 project scaffold and build the core dashboard layout.

Purpose: Establish the visual and technical foundation for the Ziptrrip-aligned UI.
Output: Working Next.js app with a functional sidebar and theme support.
</objective>

<execution_context>
@/Users/User/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/User/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js and shadcn/ui</name>
  <files>package.json, components.json, tailwind.config.ts, src/app/layout.tsx</files>
  <action>
    Initialize a new Next.js 16 project using Turbopack and Tailwind v4.
    Install essential dependencies: lucide-react, next-themes, class-variance-authority, clsx, tailwind-merge.
    Initialize shadcn/ui with the "New York" style and "Slate" base color.
    Configure `src/app/layout.tsx` with a ThemeProvider for light/dark mode support.
  </action>
  <verify>npm run dev (ensure no errors), check tailwind config for v4 patterns.</verify>
  <done>Next.js dev server starts, shadcn components can be added.</done>
</task>

<task type="auto">
  <name>Task 2: Build Dashboard Layout and Sidebar</name>
  <files>src/app/(dashboard)/layout.tsx, src/components/layout/sidebar.tsx, src/components/layout/header.tsx</files>
  <action>
    Create a `(dashboard)` route group with a layout that includes a Sidebar and Header.
    Implement a collapsible Sidebar using shadcn/ui components (Sidebar, SidebarHeader, SidebarContent, SidebarFooter).
    Add navigation links for Bookings, Hotels, Admin, and Notifications.
    Include a theme toggle in the Header.
    Match Ziptrrip's dashboard density (clean tables, professional typography).
  </action>
  <verify>Visually confirm sidebar collapse behavior and theme switching.</verify>
  <done>Main dashboard layout is functional and aesthetically aligned with PRD.</done>
</task>

</tasks>

<verification>
Check `npm run build` to ensure no environment mismatches.
Verify `src/app/page.tsx` redirects to `/bookings` or shows a dashboard overview.
</verification>

<success_criteria>
- [ ] Next.js app initialized and running.
- [ ] Sidebar Navigation is functional and collapsible.
- [ ] Light/Dark mode toggle works.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
