---
phase: 02-feedback-loop
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/(dashboard)/feedback/[bookingId]/page.tsx
  - src/features/feedback/components/feedback-form.tsx
  - src/features/feedback/components/star-rating.tsx
  - src/features/feedback/actions/submit-feedback.ts
  - src/features/feedback/lib/scoring.ts
autonomous: true
requirements: [COL-01, COL-02, COL-04, SCI-01]
must_haves:
  truths:
    - "User can rate 5 categories from 1 to 5 using stars"
    - "User can add an optional comment"
    - "Weighted score is calculated on submission"
    - "Feedback is persisted to the database"
  artifacts:
    - path: "src/features/feedback/components/star-rating.tsx"
      provides: "Accessible rating input"
    - path: "src/features/feedback/lib/scoring.ts"
      provides: "Weighted average calculation logic"
  key_links:
    - from: "src/features/feedback/actions/submit-feedback.ts"
      to: "src/features/feedback/lib/scoring.ts"
      via: "Function call before DB insert"
---

<objective>
Implement the detailed feedback collection form and weighted scoring logic.

Purpose: Collect verified guest insights and process them into quality signals.
Output: Dynamic feedback form page with database persistence.
</objective>

<execution_context>
@/Users/User/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/User/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feedback-loop/02-CONTEXT.md
@.planning/phases/02-feedback-loop/02-RESEARCH.md
@.planning/phases/02-feedback-loop/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build StarRating and FeedbackForm Components</name>
  <files>src/features/feedback/components/star-rating.tsx, src/features/feedback/components/feedback-form.tsx</files>
  <action>
    Create a reusable `StarRating` component using `lucide-react` stars.
    Build the `FeedbackForm` using React Hook Form and Zod.
    Fields: Value for Money, Service, Cleanliness, Amenities, Intent (all 1-5 stars).
    Add a text area for optional comments.
    Match Ziptrrip's clean B2B form styling.
  </action>
  <verify>Visually confirm form fields and star interaction.</verify>
  <done>Feedback UI is complete and interactive.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Scoring and Submission logic</name>
  <files>src/features/feedback/lib/scoring.ts, src/features/feedback/actions/submit-feedback.ts, src/app/(dashboard)/feedback/[bookingId]/page.tsx</files>
  <action>
    Create `calculateWeightedScore(ratings)` in `scoring.ts` (30% Cleanliness, 30% Service, etc.).
    Implement `submitFeedback` Server Action.
    Action: Validate with Zod, calculate score, insert into `feedback` table, update `booking.feedback_submitted` to true.
    Create the dynamic route `src/app/(dashboard)/feedback/[bookingId]/page.tsx`.
    Fetch hotel/booking context for the form header.
  </action>
  <verify>Submit a form and verify new row in `feedback` table with `computed_score`.</verify>
  <done>Feedback loop captures and scores data successfully.</done>
</task>

</tasks>

<verification>
Confirm `feedback_submitted` flag updates in DB after submission.
Verify `computed_score` matches the weighted weights from PRD.
</verification>

<success_criteria>
- [ ] Accessible Star Rating component.
- [ ] Validated form submission with weighted scoring.
</success_criteria>

<output>
After completion, create `.planning/phases/02-feedback-loop/02-02-SUMMARY.md`
</output>
