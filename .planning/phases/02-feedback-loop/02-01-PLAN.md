---
phase: 02-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/bookings/page.tsx
  - src/features/bookings/components/booking-table.tsx
  - src/features/bookings/actions/simulate-checkout.ts
  - src/features/bookings/components/checkout-dialog.tsx
autonomous: true
requirements: [TRG-01, TRG-02]
must_haves:
  truths:
    - "User can view a list of mock bookings"
    - "Clicking 'Simulate Checkout' shows a confirmation dialog"
    - "Confirming checkout updates status to 'completed' and 'feedback_eligible' to true"
  artifacts:
    - path: "src/features/bookings/actions/simulate-checkout.ts"
      provides: "Server action for DB status update"
    - path: "src/features/bookings/components/booking-table.tsx"
      provides: "Ziptrrip-aligned booking list"
  key_links:
    - from: "src/features/bookings/components/checkout-dialog.tsx"
      to: "src/features/bookings/actions/simulate-checkout.ts"
      via: "Server Action invocation"
---

<objective>
Build the booking simulation interface to trigger the feedback loop.

Purpose: Allow travellers (and demo users) to simulate stay completion.
Output: Working bookings page with status simulation logic.
</objective>

<execution_context>
@/Users/User/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/User/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feedback-loop/02-CONTEXT.md
@.planning/phases/02-feedback-loop/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Bookings Page and Table</name>
  <files>src/app/(dashboard)/bookings/page.tsx, src/features/bookings/components/booking-table.tsx</files>
  <action>
    Create the `bookings` page that fetches data from Supabase.
    Implement a clean `BookingTable` using shadcn components.
    Columns: Traveller, Hotel, Dates (Check-in/Out), Status.
    Show a status pill (Badge) for 'booked', 'checked_in', and 'completed'.
  </action>
  <verify>Ensure table renders with seeded data from Phase 1.</verify>
  <done>Bookings are visible in a professional dashboard table.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Checkout Simulation Action</name>
  <files>src/features/bookings/actions/simulate-checkout.ts, src/features/bookings/components/checkout-dialog.tsx</files>
  <action>
    Create a Server Action `simulateCheckout(bookingId: string)`.
    Action updates `status` to 'completed' and `feedback_eligible` to `true` in Supabase.
    Use `revalidatePath` to refresh the bookings page.
    Implement a `CheckoutDialog` using shadcn/ui Dialog to confirm the action.
    Show a "Simulate Checkout" button inline in the table for eligible rows.
    If already completed, show a "Rate Now" button/link.
  </action>
  <verify>Click simulate, confirm, and verify status change in UI and DB.</verify>
  <done>End-to-end simulation trigger is functional.</done>
</task>

</tasks>

<verification>
Check `feedback_eligible` column in Supabase after simulation.
Confirm 'Rate Now' link appears after completion.
</verification>

<success_criteria>
- [ ] Bookings table populated.
- [ ] Checkout simulation updates DB and UI state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-feedback-loop/02-01-SUMMARY.md`
</output>
