---
phase: 07-design-mockups
plan: "05"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
files_modified:
  - pencil-new.pen
  - .planning/designs/07-feedback-form-desktop.png
  - .planning/designs/07-feedback-form-validation.png
  - .planning/designs/07-feedback-form-mobile-375.png
  - .planning/designs/08-success.png
autonomous: false
requirements:
  - DSG-01

must_haves:
  truths:
    - "A Feedback Form (desktop) frame exists showing 5 category sliders, optional comment textarea, and teal submit button for Grand Hyatt Mumbai"
    - "A Feedback Form (validation) frame exists showing at least one slider error and a comment validation error with the submit button disabled"
    - "A Feedback Form (375px mobile) frame exists showing single-column layout with full-width sliders and sticky submit button — no sidebar"
    - "A Success frame exists showing a confirmation card with teal checkmark icon and hotel + score summary"
    - "All 4 frames exported as PNG to .planning/designs/"
    - "All 14 minimum PNG exports are now present in .planning/designs/"
    - "Human reviewer has confirmed overall design quality before phase is marked complete"
  artifacts:
    - path: ".planning/designs/07-feedback-form-desktop.png"
      provides: "Feedback form desktop reference"
    - path: ".planning/designs/07-feedback-form-validation.png"
      provides: "Feedback form validation state reference"
    - path: ".planning/designs/07-feedback-form-mobile-375.png"
      provides: "Feedback form mobile 375px reference"
    - path: ".planning/designs/08-success.png"
      provides: "Success confirmation page reference"
  key_links:
    - from: "App Shell frame (07-01 SUMMARY)"
      to: "Feedback Form desktop frames"
      via: "batch_design Copy (C) using shell node ID"
      pattern: "C(shell-node-id, feedback-form-frame-id)"
---

<objective>
Design the Feedback Form (3 states: desktop happy path, desktop validation, 375px mobile) and the Success screen, then export all 4 frames as PNGs. Then present the complete design set for human approval before Phase 7 is closed.

Purpose: This final plan completes the 14-frame minimum export set. The mobile form variant is the only responsive design in the system. The human checkpoint confirms the full design system is coherent before Phase 8 begins implementation.
Output: 4 frames + 4 PNG exports + human approval gate for the complete design set.
</objective>

<execution_context>
@/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-design-mockups/07-CONTEXT.md
@.planning/phases/07-design-mockups/07-RESEARCH.md
@.planning/phases/07-design-mockups/07-01-SUMMARY.md
@.planning/phases/07-design-mockups/07-02-SUMMARY.md
@.planning/phases/07-design-mockups/07-03-SUMMARY.md
@.planning/phases/07-design-mockups/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design Feedback Form (desktop happy path + desktop validation + 375px mobile)</name>
  <files>pencil-new.pen</files>
  <action>
    Open `pencil-new.pen`. Read all SUMMARY files for context (especially 07-01-SUMMARY.md for shell node ID).

    **Frame A: Feedback Form Desktop Happy Path (1440×900px)**

    Build in 3 `batch_design` calls (≤25 ops each):

    Layer 1 — Frame + Shell:
    - Create frame: name="Feedback Form - Desktop", width=1440, height=900, background=#F8FAFA
    - Copy App Shell (shell node ID from 07-01-SUMMARY.md). Note: Nav item for Bookings should be active (user came from Bookings page). Or set no active item — Claude's discretion.
    - Header breadcrumb: "Bookings / Feedback — Grand Hyatt Mumbai"

    Layer 2 — Page Header + Form Container:
    - Page title: Star icon (size=24, color=#72D3C4) + "Submit Feedback" (x=264, y=100, size=24px, bold, #181A1A)
    - Hotel context bar: x=264, y=136, white bg, rounded-xl, border 1px #E9ECEC, padding=16px 20px
      - Building icon (color=#72D3C4) + "Grand Hyatt Mumbai" (bold, size=16px) + "Mumbai, India" (size=13px, color=#717373) + score chip "4.8★ — Top Rated" (teal pill)
    - Form card: x=264, y=188, width=900, white bg, rounded-xl, border 1px #E9ECEC, padding=32px
    - Form title: "Rate Your Stay" (size=18px, bold, color=#181A1A) + "Drag each slider to rate this dimension of your experience." (size=13px, color=#717373)

    Layer 3 — Category Sliders + Submit:
    - 5 category sliders in a 2-column grid (≤25 ops):
      Each slider row (height=72px, gap=16px):
      - Label: category name (bold, size=14px) + description (size=12px, color=#717373)
      - Slider track: full width of column (rounded-full, height=6px, background=#E9ECEC) with filled portion in teal (#72D3C4)
      - Slider thumb: circle (16×16, background=#72D3C4, border 2px white, shadow)
      - Current value badge: rounded-full pill showing value (e.g., "4" in teal bg)
      Categories and example values:
        1. Value for Money — 4/5, "How much value did you receive?"
        2. Service Quality — 5/5, "Was the staff attentive and professional?"
        3. Cleanliness — 5/5, "How clean was the room and facilities?"
        4. Amenities — 4/5, "Were the hotel amenities satisfactory?"
        5. Intent to Return — 5/5, "Would you book this hotel again?" (full width, 5th item spans both columns)
    - Comment textarea: below sliders, full width of form, height=100px, border 1px #E9ECEC, rounded-xl, placeholder="Share additional comments... (optional)", resize=none
    - Submit button row: right-aligned or full-width
      - "Submit Feedback" button: rounded-full, background=#72D3C4, text=#FFFFFF, height=44px, padding=0 32px, font-weight=600

    **Frame B: Feedback Form Desktop Validation State (1440×900px)**

    Build in 2 `batch_design` calls (≤25 ops each):

    Layer 1 — Frame + Shell (same breadcrumb as happy path):
    - Create frame: name="Feedback Form - Validation", width=1440, height=900, background=#F8FAFA
    - Copy App Shell, set same nav state as Frame A

    Layer 2 — Form with Error States:
    - Same form structure as happy path
    - Validation errors to show:
      - Cleanliness slider set at 1/5 (only 1/5 filled teal) with red error label below: "Score required — move the slider to rate." (size=12px, color=#B03030, background=#FDE8E8, rounded-sm, padding=4px 8px)
      - Comment textarea: empty, red border (border color #F08080), error message below: "Comment is required when any score is below 3." (size=12px, color=#B03030)
      - Submit button: disabled state — background=#E9ECEC, text=#717373, cursor-not-allowed

    **Frame C: Feedback Form 375px Mobile (375×812px — iPhone viewport)**

    This frame has NO sidebar. Single-column layout. Build in 3 `batch_design` calls (≤25 ops each):

    Layer 1 — Frame + Mobile Header:
    - Create frame: name="Feedback Form - Mobile 375px", width=375, height=812, background=#F8FAFA
    - Mobile header bar: x=0, y=0, width=375, height=60px, background=#FFFFFF, border-bottom 1px #E9ECEC
      - Back arrow icon (←, x=16, y=20, size=20, color=#181A1A)
      - Title "Submit Feedback" centered (size=16px, bold, color=#181A1A)
    - Hotel context bar: x=12, y=68, width=351, height=52, white bg, rounded-xl, border 1px #E9ECEC, padding=12px
      - "Grand Hyatt Mumbai" bold + teal "4.8★" pill right-aligned

    Layer 2 — Sliders (single column, full width, ≥44px touch targets):
    - Form container: x=12, y=128, width=351, white bg, rounded-xl, border 1px #E9ECEC, padding=16px
    - 5 sliders stacked vertically, each row height=72px:
      - Label: category (bold, size=14px)
      - Slider track: full width (351-32=319px), height=8px (slightly thicker for touch), teal fill
      - Slider thumb: 24×24 (larger for touch, ≥44px tap target with padding)
      - Value chip on right: current value (teal pill)
    - Comment textarea: x=12, y=500 (approx), width=351, height=80px, rounded-xl, border 1px #E9ECEC

    Layer 3 — Sticky Submit Button:
    - Sticky footer bar: x=0, y=752 (812-60), width=375, height=60px, background=#FFFFFF, border-top 1px #E9ECEC, padding=8px 16px
    - "Submit Feedback" button: full width (343px), rounded-full, background=#72D3C4, text=#FFFFFF, height=44px, font-weight=600

    After each frame, call `get_screenshot()` to validate. For the mobile frame specifically, confirm:
    - No sidebar visible
    - Sliders are full-width (351px)
    - Sticky submit button is at the bottom
    - Touch targets look large enough (slider thumb is 24×24+)
  </action>
  <verify>
    <automated>Call `get_screenshot()` on each of the 3 Feedback Form frames. Confirm: desktop frames show sidebar, 2-column slider grid visible, submit button rounded-full teal. Mobile frame shows NO sidebar, single-column layout, sticky footer submit button.</automated>
    <manual>Validation frame: Confirm comment textarea has red border and error message below it. Confirm at least one slider shows an error state. Confirm submit button is grey/disabled.</manual>
  </verify>
  <done>3 Feedback Form frames designed and screenshot-validated. Desktop happy path shows 5 sliders + comment + rounded-full teal submit. Validation state shows red errors + disabled submit. Mobile 375px shows single-column, no sidebar, sticky submit.</done>
</task>

<task type="auto">
  <name>Task 2: Design Success screen and export all 4 final frames</name>
  <files>
    pencil-new.pen
    .planning/designs/07-feedback-form-desktop.png
    .planning/designs/07-feedback-form-validation.png
    .planning/designs/07-feedback-form-mobile-375.png
    .planning/designs/08-success.png
  </files>
  <action>
    **Frame D: Success Screen (1440×900px)**

    The Success screen is Claude's discretion on confirmation card design (per CONTEXT.md). Build in 2 layers (≤25 ops total):

    Layer 1 — Frame + Shell:
    - Create frame: name="Success", width=1440, height=900, background=#F8FAFA
    - Copy App Shell. Breadcrumb: "Feedback / Submitted"

    Layer 2 — Confirmation Card (centered in content area):
    - Centered card: width=480px, centered horizontally (x=480, y=160), white bg, rounded-2xl, border 1px #E9ECEC, shadow-md, padding=48px
    - Teal success icon circle: 72×72px, background=#E8F9F7, centered at card top, containing a checkmark icon (color=#72D3C4, size=40)
    - Heading: "Feedback Submitted!" (size=24px, bold, color=#181A1A, centered, y=card-top+100)
    - Subtext: "Thank you, Priya Sharma. Your feedback has been recorded." (size=15px, color=#717373, centered)
    - Hotel context box: background=#F2F4F4, rounded-xl, padding=16px 24px, margin=24px 0
      - "Grand Hyatt Mumbai" bold + "4.8★ — Your submitted score" (teal, size=14px)
      - Score summary: "Cleanliness 5.0 · Service 5.0 · Value 4.0 · Amenities 4.0 · Intent 5.0" (size=12px, color=#717373)
    - Score badge: large centered "4.7 / 5.0" (size=36px, bold, color=#181A1A) + "Weighted Score" label (size=12px, color=#717373)
    - Primary action: "Return to Hotels" button (rounded-full, teal bg, height=44px, full-width or 240px centered)
    - Secondary: "View All Bookings" link (text-link, teal color, centered below button)

    After building, call `get_screenshot()` on Success frame. Confirm: confirmation card is centered, teal checkmark circle is visible, hotel name and score summary are present.

    **Export all 4 final frames:**

    Call `export_nodes`:
    - nodeIds: [feedback-form-desktop-id, feedback-form-validation-id, feedback-form-mobile-id, success-id]
    - format: "PNG"
    - folder: "/Users/User/Documents/feedback-sys/.planning/designs/"

    After export, verify final PNG count. Expected total: 14 minimum files.
    Run: `ls /Users/User/Documents/feedback-sys/.planning/designs/*.png | wc -l`
    Expected: 13+ PNG files (01-shell may not be exported if Plan 01 skipped it, adjust count accordingly — minimum is 13 named files matching the RESEARCH.md file structure).

    If 01-shell.png is missing, export the App Shell frame now:
    `export_nodes([shell-frame-id], format="PNG", folder="/Users/User/Documents/feedback-sys/.planning/designs/")`
  </action>
  <verify>
    <automated>Run `ls /Users/User/Documents/feedback-sys/.planning/designs/*.png` to list all exported PNGs. Confirm at least 13 files exist (or 14 if shell was exported). Call `get_screenshot()` on Success frame to confirm confirmation card is visible.</automated>
    <manual>Verify the confirmation card shows: teal checkmark circle, hotel name "Grand Hyatt Mumbai", traveller name "Priya Sharma", score summary. The card should feel like a genuine product page, not a placeholder.</manual>
  </verify>
  <done>Success frame designed and screenshot-validated. All 4 final frames exported as PNG. Total PNG count is 13-14 files in .planning/designs/. Phase 7 PNG export set is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human approval of complete Phase 7 design set</name>
  <action>
    Present the complete Phase 7 design set for human review. All automation is complete — the human reviews the exported PNGs and .pen file, then either approves or describes specific issues.

    What was built:
    All 14 Phase 7 mockup frames designed in `pencil-new.pen` and exported as PNG files to `.planning/designs/`:

    01-shell.png — App Shell (sidebar + header)
    02-hotels-happy.png — Hotels grid (8 hotel cards, teal badge palette)
    02-hotels-empty.png — Hotels empty state
    03-bookings-happy.png — Bookings table (6 rows, action buttons)
    03-bookings-empty.png — Bookings empty state
    04-admin-happy.png — Admin dashboard (metric cards, flagged table, activity feed)
    05-settings-happy.png — Settings form (weights 30/30/20/20)
    05-settings-no-config.png — Settings empty state
    05-settings-save-error.png — Settings error state
    06-notifications-grid.png — Notifications 2x2 grid (Email/Slack/Teams/WhatsApp with positive+URGENT per channel)
    07-feedback-form-desktop.png — Feedback Form desktop happy path
    07-feedback-form-validation.png — Feedback Form validation errors
    07-feedback-form-mobile-375.png — Feedback Form 375px mobile
    08-success.png — Success confirmation card

    How to verify:
    1. Open `pencil-new.pen` in Pencil MCP to browse all frames interactively.
    2. Review the 14 PNG exports in `.planning/designs/` — each should be visually distinct.
    3. Specifically check:
       - Hotels badges use teal-derived colors (NOT hardcoded green/slate/orange/red)
       - Notifications 2x2 grid: Slack is dark, Teams is MS-card style, WhatsApp is green-beige, Email is email-client chrome
       - Feedback Form mobile has NO sidebar and has sticky submit button at bottom
       - Admin metric cards use design system tokens (no hardcoded yellow/blue)
       - Success card shows "Priya Sharma" and "Grand Hyatt Mumbai" with teal checkmark
    4. One revision round is permitted per screen — note specific frame names and issues.
  </action>
  <verify>
    <automated>Run `ls /Users/User/Documents/feedback-sys/.planning/designs/*.png | wc -l` and confirm 13-14 PNG files exist. All automation was completed in Tasks 1-2.</automated>
    <manual>Human reviews all PNG exports and the live .pen file via Pencil MCP. Approval unlocks Phase 8 implementation. Raise issues per frame name for the one permitted revision round.</manual>
  </verify>
  <done>Human has reviewed all 14 frame exports and either typed "approved" or described specific revision issues per frame. Phase 7 is complete only after this confirmation. Phase 8 can begin on approved screens immediately.</done>
  <resume-signal>Type "approved" to confirm Phase 7 complete and Phase 8 can begin. Or describe specific issues per frame name (e.g., "Hotels badges: re-do colors — still showing green"). Maximum one revision round per screen.</resume-signal>
</task>

</tasks>

<verification>
1. 13-14 PNG files exist in .planning/designs/.
2. All frames screenshot-validated before export.
3. Human has reviewed and approved the complete design set.
4. No Lorem Ipsum — all Indian corporate travel seed data used.
5. Teal #72D3C4 used only for accents, icons, focus elements — NOT body text.
6. All pill/badge elements are rounded-full per DSG-05 requirement.
</verification>

<success_criteria>
- 14 frames designed and PNG-exported (covers DSG-01)
- Notifications 2x2 grid with native channel chrome covers DSG-02
- Human approval gate confirms design quality before Phase 8
- .pen file and PNGs both committed and available for Phase 8 to reference
</success_criteria>

<output>
After completion and human approval, create `.planning/phases/07-design-mockups/07-05-SUMMARY.md` containing:
- Final PNG file listing with confirmation all 14 exist
- Human approval status and any revision notes
- Key design decisions made (Success card layout, mobile form sticky footer approach)
- Phase 8 handoff notes: which .pen file path to open, which frame names map to which Phase 8 components
</output>
