---
phase: 07-design-mockups
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pencil-new.pen
autonomous: true
requirements:
  - DSG-01

must_haves:
  truths:
    - "A .pen file exists at pencil-new.pen"
    - "Ziptrrip design tokens (primary #72D3C4, foreground #181A1A, card #FFFFFF, border #E9ECEC, accent #E8F9F7, destructive #F08080) are registered as named variables in the .pen file"
    - "An App Shell frame exists showing the 240px sidebar (logo, 5 nav items, footer) and 80px header (breadcrumb, Business/Personal toggle, theme button)"
    - "A screenshot of the App Shell frame has been taken and visually confirmed correct before proceeding"
  artifacts:
    - path: "pencil-new.pen"
      provides: "Single source of truth .pen file with design tokens and App Shell frame"
  key_links:
    - from: "App Shell frame"
      to: "All subsequent screen frames (Plans 02-05)"
      via: "batch_design Copy (C) operation embedding shell-frame-id"
      pattern: "C(shell-frame-id, ...)"
---

<objective>
Create the .pen design file, register the Ziptrrip design system tokens, and design the App Shell frame that all subsequent screens will embed.

Purpose: This plan is the foundation of the entire design phase. The App Shell frame (sidebar + header) is the shared chrome that every screen copies. Getting it right here means zero drift across all 14 frames.
Output: `pencil-new.pen` with design tokens set and an App Shell frame ready for copying.
</objective>

<execution_context>
@/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-mockups/07-CONTEXT.md
@.planning/phases/07-design-mockups/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .pen file and register Ziptrrip design tokens</name>
  <files>pencil-new.pen</files>
  <action>
    Execute the session-start sequence using Pencil MCP tools:

    Step 1 — Check editor state:
    Call `get_editor_state()`. If `pencil-new.pen` is not active, call `open_document("/Users/User/Documents/feedback-sys/pencil-new.pen")`. If the file does not exist yet, pass "new" to create it, then immediately use `export_nodes` or note the file was placed at the correct path.

    Step 2 — Load design guidelines:
    Call `get_guidelines(topic="web-app")` to load web-app layout rules into context.

    Step 3 — Load style guide:
    Call `get_style_guide_tags()` to discover available tags, then call `get_style_guide(tags=[...], name="...")` selecting tags appropriate for a B2B SaaS dashboard with teal accent (corporate, dashboard, minimal, clean).

    Step 4 — Register Ziptrrip design tokens:
    Call `set_variables` with these exact values from globals.css:
    ```
    primary:            #72D3C4
    primary-foreground: #FFFFFF
    background:         #F8FAFA
    foreground:         #181A1A
    card:               #FFFFFF
    muted:              #F2F4F4
    muted-foreground:   #717373
    accent:             #E8F9F7
    accent-foreground:  #267268
    destructive:        #F08080
    border:             #E9ECEC
    radius:             12
    ```

    Step 5 — Verify tokens registered:
    Call `get_variables()` and confirm all 12 variable names appear in the response.

    Note: Do NOT use raw hex values in subsequent batch_design calls — reference the registered variable names.
  </action>
  <verify>
    <automated>Call `get_variables()` via Pencil MCP. Confirm response contains: primary, primary-foreground, background, foreground, card, muted, muted-foreground, accent, accent-foreground, destructive, border, radius — all 12 variables present.</automated>
  </verify>
  <done>The .pen file exists at pencil-new.pen and all 12 Ziptrrip design tokens are registered as named variables.</done>
</task>

<task type="auto">
  <name>Task 2: Design the App Shell frame (sidebar + header)</name>
  <files>pencil-new.pen</files>
  <action>
    Design the App Shell as a standalone named frame. This frame is the shared chrome copied into every screen. Use `find_empty_space_on_canvas` first to locate placement, then execute 2-3 `batch_design` calls (≤25 ops each) to build the frame in layers.

    **Frame dimensions:** 1440×900px (desktop viewport)

    **Layer 1 — Frame and Sidebar (≤25 ops):**
    - Root frame: name="App Shell", width=1440, height=900, background=#F8FAFA
    - Sidebar panel: width=240, height=900, x=0, y=0, background=#FFFFFF, border-right 1px #E9ECEC
    - Logo area (y=16 to y=64): teal 32×32 square (rounded, #72D3C4, x=16, y=16) + "ziptrrip" bold text (x=56, y=20, color=#181A1A, font-weight=700, size=16px) + "FEEDBACK INTEL" text (x=56, y=38, color=#717373, size=10px, letter-spacing=widest, uppercase)
    - Divider line: y=68, x=16, width=208, height=1, color=#E9ECEC
    - Nav section label: "NAVIGATION" at y=80, x=20, size=10px, color=#717373, uppercase, tracking-widest
    - Nav items (5 items, h=44px each, gap=4px, starting y=96): Dashboard (active state), Bookings, Hotels, Notifications, Settings
    - Active nav item (Dashboard): background=#E8F9F7 (accent), rounded-lg, icon+text color=#267268 (accent-foreground)
    - Inactive nav items: icon+text color=#717373 (muted-foreground), hover would be muted bg (show muted state on one item for reference)
    - Nav item text: size=14px, font-weight=500

    **Layer 2 — Sidebar Footer and Header (≤25 ops):**
    - Sidebar footer card: x=12, y=840, width=216, height=52, background=#E8F9F7 (accent), border 1px #72D3C4/20, rounded-lg
    - Footer icon: small teal checkmark or shield icon at x=24, y=858
    - Footer text line 1: "Verified Stays Only" at x=44, y=854, size=12px, color=#267268, font-weight=600
    - Footer text line 2: "B2B Corporate Travel" at x=44, y=870, size=11px, color=#717373
    - Header bar: x=240, y=0, width=1200, height=80, background=#F8FAFA/80 (translucent), border-bottom 1px #E9ECEC
    - Header left section: SidebarTrigger icon button (24×24, x=256, y=28, color=#717373) + vertical divider (1×24, x=290, y=28, color=#E9ECEC) + breadcrumb area (x=302, y=20)
    - Breadcrumb line 1: "PROJECT" at x=302, y=28, size=11px, color=#717373, uppercase
    - Breadcrumb line 2: "Feedback Intelligence" at x=302, y=44, size=14px, color=#181A1A, font-weight=600
    - Header right section: Business/Personal pill toggle (x=1220, y=24, width=200, height=32, rounded-full, background=#F2F4F4) with "Business" active pill (#FFFFFF, shadow) and "Personal" inactive text
    - Theme toggle icon button: x=1428, y=24, width=32, height=32, rounded-full, border 1px #E9ECEC, sun/moon icon color=#717373

    **Layer 3 — Content area placeholder (≤10 ops):**
    - Content zone label: "[ Screen Content ]" centered in the content area (x=240+, y=80+), muted text, size=20px, color=#E9ECEC — this signals where screen-specific content goes when the shell is copied into screen frames. Remove this placeholder in actual screen frames.

    After all layers, call `get_screenshot()` on the App Shell frame node ID. Review visually:
    - Sidebar must be 240px, white, with visible nav items
    - Active Dashboard item must show teal (accent) background
    - Header must span the full right 1200px, 80px tall
    - No raw hex overrides of design tokens visible (confirm using accent colors, not #72D3C4 directly in large fills)
    - Teal color #72D3C4 appears ONLY on: logo square, active icon/text color, sidebar footer border accent — NOT as body text

    If visual issues are found, use `batch_design` Update (U) or Replace (R) operations to correct them before proceeding.
  </action>
  <verify>
    <automated>Call `get_screenshot()` on the App Shell frame. Visually confirm: sidebar is 240px wide with 5 nav items, Dashboard is active with teal accent, header spans remaining 1200px at 80px tall, Business/Personal toggle is visible, no Lorem Ipsum text present.</automated>
    <manual>Check that "ziptrrip" wordmark and "FEEDBACK INTEL" subtext are readable. Confirm "Verified Stays Only" footer card is visible at sidebar bottom.</manual>
  </verify>
  <done>App Shell frame exists in the .pen file with correct sidebar (logo, 5 nav items, footer), header (breadcrumb, toggles), and has been screenshot-validated. The frame's node ID is known for use in Copy operations in Plans 02-04.</done>
</task>

</tasks>

<verification>
1. `pencil-new.pen` file exists and opens via Pencil MCP `open_document`.
2. `get_variables()` returns all 12 Ziptrrip tokens.
3. A named "App Shell" frame exists on the canvas at 1440×900px.
4. Screenshot of App Shell shows correct sidebar and header chrome.
5. No Lorem Ipsum or placeholder data in the App Shell.
</verification>

<success_criteria>
- .pen file created at the correct path
- 12 design tokens registered
- App Shell frame screenshot-validated, ready for Copy embedding in Plans 02-04
- App Shell node ID documented in SUMMARY.md for downstream plans
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-mockups/07-01-SUMMARY.md` containing:
- The .pen file path
- The App Shell frame node ID (critical — Plans 02-04 need this to Copy the shell)
- Confirmation of all 12 design tokens registered
- Screenshot description noting any visual decisions made
</output>
